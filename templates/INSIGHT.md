---
number: NN
title: "<one-line title, the rule in a single clause>"
---

# Insight #NN, <one-line title>

Each survey extracts one numbered Insight. The surveys produce data, the
insights produce the methodology. Keep this file to the four parts below. State
the rule, not the war story.

## The class of mistake

<The error this insight names, stated generally. Not "host X did Y" but the
repeatable category: the assumption that fails, the default that misleads, the
verification step that gets skipped. One short paragraph. A reader on a different
survey should recognize the same trap.>

## The default it ties to

<The generalizable operating default this corrects or reinforces. State it as a
standing rule. Examples of the shape: "auth-on-default strengthens across OSS
generations under disclosure pressure", "a banner confirms liveness, not schema",
"a dork can select a biased subpopulation". One or two sentences.>

## The verification rule that catches it

<The concrete check that would have caught the mistake, written so it can be run.
What to probe, what response promotes the claim, what response leaves it a
candidate. Tie to the rung grid where it applies: state which inner / outer rung
the check establishes and which it does not. This is the load-bearing part, the
verification stage is where most insights live.>

## Survey that produced it

<Survey slug only. No raw target data, no IPs, no host counts that fingerprint a
specific operator. The slug points to the case study; the case study holds the
evidence under its own redaction boundary.>

---

_Cross-link related insights by number. If this one extends or refines a prior
insight, say which and how in one line._
